#include "holberton.h"
#include <stdlib.h>

/**
 * _abs - absolute value of an integer
 * @a: entero
 * main - Return
 * Description: islower
 * Return: 0 or 1
 */
int _abs(int a)
{
if (a < 0)
{
a = a * -1;
}
return (a);
}
