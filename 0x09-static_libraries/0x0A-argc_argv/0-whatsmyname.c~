#include "holberton.h"
#include <stdio.h>

/**
 * main - entry point
 * return - 0 if success
 * @argc: variable
 * @argv: variable
 */
int main(int argc, char **argv)
{
while (argc--)
{
printf("%s\n", *argv++);
}
return (0);
}
