#include "holberton.h"


/**
 * print_numbers - prints every minute of the day
 * main - Return
 * Return: void
 */
void print_numbers(void)
{
int numeros;
for (numeros = 48; numeros <= 57; numeros++)
{
_putchar (numeros);
_putchar (10);
}
return;
}
