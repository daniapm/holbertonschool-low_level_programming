#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
int numeros;
for (numeros = 0; numeros < 10; numeros++)
{
putchar(numeros);
}
putchar('\n');
return (0);
}
