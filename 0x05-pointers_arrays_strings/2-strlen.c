#include "holberton.h"
#include <string.h>

/**
 * _strlen - Write a function that returns the length of a string.
 * @s: variable
 * main - Return
 * Return: always 0
 */
int _strlen(char *s)
{
int len;
len = strlen(s);
return (len);
}
