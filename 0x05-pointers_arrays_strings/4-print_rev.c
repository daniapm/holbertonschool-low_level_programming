#include "holberton.h"

/**
 * print_rev - rever
 * @s: variable
 * main - Return
 * Return: void
 */
void print_rev(char *s)
{
int i;
int contador;
contador = 0;
while (contador[s] != 0)
{
contador++;
}
for (i = contador; i >= 0; i--)
{
_putchar (s[i]);
}
_putchar (10);
return;
}
