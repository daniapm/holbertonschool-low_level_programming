#include "holberton.h"

/**
 * print_rev - rever
 * @s: variable
 * main - Return
 * Return: void
 */
void print_rev(char *s)
{
int i;
int len;
len = 0;
while (len[s] != 0)
{
len++;
}
for (i = len; i >= 0; i--)
{
_putchar(s[i]);
}
printf("\n");
return;
}
