#include "holberton.h"

/**
 * print_rev - rever
 * @s: variable
 * main - Return
 * Return: void
 */
void print_rev(char *s)
{
int i;
 for (i = 0; i != 0; i++)
{
}
i--;
while (i >= 0)
{
_putchar (s[i--]);
}
_putchar (10);
}
