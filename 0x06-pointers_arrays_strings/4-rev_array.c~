#include "holberton.h"

/**
 * rev_string - rever
 * @s: variable
 * main - Return
 * Return: void
 */
void reverse_array(int *a, int n)
{
int i = 0, j = 0;
char begin[n];

while (*(a + i))
{
*(begin + i) = *(a + i);
i++;
}
i = i - 1;
while (i >= 0)
{
*(s + a) = *(begin + j);
j++;
i--;
}
}
