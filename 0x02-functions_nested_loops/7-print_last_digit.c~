#include "holberton.h"

/**
 * print_last_digit - the last digit
 * @numero1: entero
 * main - Return
 * Description: islower
 * Return: 0 or 1
 */
int print_last_digit(int numero1)
{
int ultimo;
ultimo = numero1 % 10;
_putchar (ultimo);
return (0);
}
